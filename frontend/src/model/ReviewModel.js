class ReviewModel {
   constructor(idReview, content, ratingPoint, timestamp) {
      this.idReview = idReview;
      this.content = content;
      this.ratingPoint = ratingPoint;
      this.timestamp = timestamp;
   }
}

export default ReviewModel;

class ImageModel {
   constructor(idImage, nameImage, thumbnail, urlImage, dataImage) {
      this.idImage = idImage;
      this.nameImage = nameImage;
      this.thumbnail = thumbnail;
      this.urlImage = urlImage;
      this.dataImage = dataImage;
   }
}

export default ImageModel;
